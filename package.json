{
  "name": "extends_stack",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "init": "npm install && cd basestack && npm install && cd .. && cd somestack && npm install && cd ..",
    "test": "cd somestack && npm test"
  },
  "dependencies": {
    "@aws-cdk/core": "1.64.1"
  }
}
